##Install composer and create project using the below comands
apt update && apt install composer
composer create-project laravel/laravel Freshcery

generating optimized autoload files
> Illuminate\Foundation\ComposerScripts::postAutoloadDump
> @php artisan package:discover --ansi

   INFO  Discovering packages.  

  laravel/pail ................................................................................................................................ DONE
  laravel/sail ................................................................................................................................ DONE
  laravel/tinker .............................................................................................................................. DONE
  nesbot/carbon ............................................................................................................................... DONE
  nunomaduro/collision ........................................................................................................................ DONE

  nunomaduro/termwind ......................................................................................................................... DONE

81 packages you are using are looking for funding.
Use the `composer fund` command to find out more!
> @php artisan vendor:publish --tag=laravel-assets --ansi --force

   INFO  No publishable resources for tag [laravel-assets].  

No security vulnerability advisories found
> @php artisan key:generate --ansi

   INFO  Application key set successfully.  

> @php -r "file_exists('database/database.sqlite') || touch('database/database.sqlite');"
> @php artisan migrate --graceful --ansi

   WARN  could not find driver (Connection: sqlite, SQL: select exists (select 1 from "main".sqlite_master where name = 'migrations' and type = 'table') as "exists").  

┌─[hulbert@Intel5]─[/var/www/html]
└──╼ $

##Fixing the above error.Do the belo fixes
sudo apt update
sudo apt install php8.2-sqlite3
If you’re not sure of the exact PHP minor version:

php -v

2️⃣ Verify the extension is enabled

Run:

php -m | grep -i sqlite

You must see:

pdo_sqlite
sqlite3

3️⃣ Restart your PHP service (important)

If using Laravel’s built-in server only:

# usually not needed, but safe
php artisan serve


If you’re using Apache:

sudo systemctl restart apache2


If using PHP-FPM:

sudo systemctl restart php8.2-fpm

4️⃣ Re-run migrations cleanly

From your Laravel project root:

php artisan migrate:fresh


Expected result:

No “could not find driver”

Tables created successfully

5️⃣ Start Laravel again
php artisan serve


Open:

http://127.0.0.1:8000


✅ The app should now load without a 500 error.
#Install authentication system
composer require laravel/ui
php artisan ui bootstrap --auth
npm install && npm run dev
#VITE
vite is a bundler
executes every editing we perform on the views
npm install && npm run dev


##Revert and remove composer packages and also install specific laravel version 
1️⃣ Remove laravel/ui package
composer remove laravel/ui
Confirm:

composer show | grep laravel/ui
2️⃣ Remove generated auth routes & files
Delete auth routes (if present)

Open:

routes/web.php


Remove anything like:

Auth::routes();
Or imports like:

use Illuminate\Support\Facades\Auth;
Delete auth views
rm -rf resources/views/auth
rm -rf resources/views/layouts
Delete HomeController (Laravel UI artifact)
rm -f app/Http/Controllers/HomeController.php
3️⃣ Remove Bootstrap frontend leftovers
rm -rf resources/sass
rm -rf resources/js
rm -rf node_modules
rm -f package-lock.json
rm -f package.json
rm -f vite.config.js
4️⃣ Clear caches
php artisan optimize:clear
check laravel version
Verify Laravel version
php artisan --version
✅ The CORRECT way to get Laravel 10.16.x (guaranteed)
🔑 Method 1 (BEST & safest – use this)

Create a Laravel 10 app, then pin the framework version.

composer create-project laravel/laravel Freshcery "10.*"
cd Freshcery


Now force exact framework version:

composer require laravel/framework:10.16.1 --no-update
composer update


Verify:

php artisan --version


✅ You should now see:

Laravel Framework 10.16.1


This is exactly what your tutor is running, even if he didn’t say it.
After this: install Laravel UI (matching tutor)

Now that versions align:

composer require laravel/ui:^4.2
php artisan ui bootstrap --auth
npm install
npm run dev
php artisan serve


Now:

Same Laravel generation

Same auth scaffolding

Same UI

Same behavior

Everything will finally rhyme.
composer require laravel/ui:^4.2
php artisan ui bootstrap --auth
#Mkae db migrations
php artisan migrate
php artisan make:model Product/Category
php artisan make:controller Products/ProductsController
$php artisan make:model Product/Product
php artisan make:model Product/Cart
[hulbert@Intel5]─[/var/www/html/Freshcery]
└──╼ $php artisan make:model Product/Order

   INFO  Model [app/Models/Product/Order.php] created successfully.  
middlewares are just some checkpoints that reinforce  some rules
php artisan make:middleware CheckForPrice
php artisan make:controller Users/UsersController

php artisan make:controller Admins/AdminsController

✅ After editing config — run these commands

Open your project terminal and run:

php artisan config:clear
php artisan cache:clear
php artisan optimize:clear


If using Sail or Valet or built-in server, restart it.

php artisan serve
